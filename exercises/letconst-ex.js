const PI = 3.14;

//var can be re-declared and let cannot
//var can be re-declared and re-assigned, and const cannot do either.
//let can be re-assigned, but const cannot.
//variable hoisting is when a variable can be declared after it has been used already, only works with var - not let and const.